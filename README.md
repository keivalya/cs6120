# vla-lang-grounding

Autonomous VLA language-grounding study. **CLAUDE.md is the source of truth** —
read it before running anything. RQ1: do VLAs causally use the instruction?
RQ2: are they fragile to meaning-preserving paraphrase?

Layout per CLAUDE.md §4. Backbone: `LIBERO-Para/` (cloned). Gates tracked with the
project owner; see CLAUDE.md §12 guardrails (no fabricated numbers, scene provably
fixed, causal claims only on LIBERO-Goal).
