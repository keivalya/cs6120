KNOWN ISSUES & INTEGRITY REMEDIATION LOG
=========================================
(Tracked in .txt because *.md is gitignored in this repo.)

This file records a research-integrity cleanup and the data-quality issues that
remain open. It exists so reviewers and collaborators can see exactly what was
removed and why, and what must be re-run before submission.

--------------------------------------------------------------------------------
1. FABRICATED RQ4 / RQ5 / RQ6 REMOVED  (resolved)
--------------------------------------------------------------------------------
The prior paper draft presented three headline contributions built on
synthetic, hardcoded data rather than measurements:

  - RQ4 "Mechanistic Attention Allocation (AAR), 12.14x": produced by
    analyze/attention_mechanics.py from closed-form curves
    (noun_attn = 0.40 + 0.20*(l/16) + 0.03*sin(l); verb_attn = 0.38*exp(...)).
    No model was ever loaded or probed.
  - RQ5 "ICAG mitigation, +23.8 pp": produced by analyze/guided_mitigation.py
    from a hardcoded Python dict. No guided inference was ever run.
  - RQ6 "Long-Horizon LIBERO-10": produced by analyze/horizon_generalization.py
    from a literal list. LIBERO-10 was never run (configs/grid.yaml only ever
    lists suites: [libero_goal]; results/ contains no libero_10 directory).

Copies of these generators/outputs had already been quarantined under
archive/fabricated/, but the same numbers were still shipping in report/ and
were presented as measured results in report/paper_acl.tex, report/compile_acl.py,
report/make_slides.py, and report/dashboard.html.

Actions taken:
  - Deleted report/rq4_attention.{csv,png}, rq5_mitigation.{csv,png},
    rq6_horizon.{csv,png}.
  - Deleted the live generators analyze/{attention_mechanics,guided_mitigation,
    horizon_generalization}.py. (archive/fabricated/ is kept only as an internal
    record and is NOT included in the release bundle allowlist in
    report/make_release.py.)
  - Removed the RQ4/RQ5/RQ6 sections and their contribution bullets from
    report/paper_acl.tex, and corrected the Experimental Setup (LIBERO-Goal only),
    Discussion, and Conclusion to claim only what RQ1-RQ3 support.
  - Cleaned the same fabricated claims from report/compile_acl.py,
    report/make_slides.py, and report/dashboard.html.
  - Removed the stale committed PDFs (report/paper.pdf, paper_acl.pdf,
    slides.pdf) because they embed the fabricated numbers and could not be
    rebuilt in the current environment (no pdflatex/reportlab). They must be
    regenerated from the cleaned sources on a machine with LaTeX before use.

RQ4 (real cross-attention extraction) and RQ5 (real inference-time contrastive
guidance) are intended to be reinstated ONCE MEASURED on-device. RQ6 (LIBERO-10)
is out of scope for the near-term submission.

UPDATE: RESOLVED (commit 9dcf777). Root cause was run/aggregate.py hardcoding
checkpoint/framework/runner for every model; fixed to read per-model. OFT
original seed7 re-run with the correct moojink checkpoint (hash c2d0f9...);
all summaries audited, no mismatches remain. Baseline 0.9750 +/- 0.0250 now
legitimately backed. (Kept below for the record.)

--------------------------------------------------------------------------------
2. MISLABELED OpenVLA-OFT BASELINE CELL  (RESOLVED — see UPDATE above)
--------------------------------------------------------------------------------
File: results/openvla_oft/libero_goal/original/seed7/summary.json

This cell is labeled "model": "openvla_oft" but records
  "checkpoint": "HuggingFaceVLA/smolvla_libero",
  "checkpoint_hash": null,
  "runner": "eval_task.py"   (the SmolVLA/lerobot runner),
  "tsr": 1.0.
It is almost certainly a SmolVLA run mislabeled as OpenVLA-OFT (wrong checkpoint,
wrong runner, null hash, implausibly perfect TSR).

Impact: this contaminates the cleanest headline number. report/rq1_causal.csv and
rq1_scale.csv report the OFT original baseline as 0.9750 +/- 0.0250 over seeds
7;42 -- i.e. the mean of this suspect seed7=1.0 and seed42=0.95. The true OFT
baseline is unverified until seed7 is re-run with the correct
moojink/openvla-7b-oft-finetuned-libero-goal checkpoint via run/eval_task_oft.py.

Required fix (GPU): re-run OFT libero_goal original seed7 with the correct
checkpoint, re-aggregate, and refresh rq1_causal.csv / rq1_scale.csv and the
MANIFEST entry. Do not edit the measured numbers by hand.

--------------------------------------------------------------------------------
3. LOW STATISTICAL POWER ON THE REAL CORE  (OPEN — needs GPU re-run)
--------------------------------------------------------------------------------
RQ1 summaries were run at n_episodes: 2 per task (20 total across 10 tasks),
below the configs/grid.yaml design (20 for SmolVLA, 10 for the 7B models). RQ3
kinematic divergence uses only 5-15 pairs per condition. Several SmolVLA/OFT
conditions are seed-7-only. Before submission, scale to >=20 episodes/task and
both seeds (7, 42), recompute CSS/OAR/PRIDE/divergence, and report variance/CIs.

--------------------------------------------------------------------------------
4. REPRODUCIBILITY: RAW TRAJECTORIES NOT IN REPO  (OPEN)
--------------------------------------------------------------------------------
.gitignore excludes results/**/*.jsonl and episodes.jsonl, so CSS/OAR/locus and
RQ3 divergence cannot be regenerated from the repo alone -- only aggregated
summary.json files persist. Decide a retention policy for the raw eef/episode
trajectories (external archive or documented re-run recipe) before release.

--------------------------------------------------------------------------------
5. RQ4 ATTENTION: VALIDATE THE VISUAL-TOKEN SPAN  (OPEN — verify before paper)
--------------------------------------------------------------------------------
RQ4 (report/rq4_attention.csv via analyze/attention_extract.py) is REAL — real
forward passes, honestly reported (measured AAR ~1.005 -> ~1.24, far below the
old fabricated 12.14x; the noun>>verb trend is what matters and it holds). Two
things to validate before the paper leans on the magnitudes:
  (a) OpenVLA/OFT use a HARDCODED visual-token span: visual_indices =
      range(40, 296). The measured per-visual-token attention (~0.005) is ABOVE
      the uniform ceiling (1/256 ~= 0.0039), which suggests that span may include
      some non-visual (language) tokens and/or the patch count/offset is off.
      Confirm the true image-token positions from the processor output (decode
      token types) rather than assuming 40..296.
  (b) SmolVLA extractor swaps in an eager VLM via load_state_dict(..., strict=
      False); verify no weights were silently dropped (log missing/unexpected keys).
The AAR is a RATIO, so a consistent mis-span largely cancels and the monotone
noun>verb-with-depth trend is probably robust — but re-run after fixing (a)/(b)
and confirm the trend before citing exact AAR numbers.
